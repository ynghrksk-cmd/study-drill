# study-drill（中1数学・理科・国語・社会・英語ドリル）

中学1年生の息子が使う勉強ドリルです。数学（index.html）・理科（science.html）・国語（kokugo.html）・社会（shakai.html）・英語（eigo.html）の5つのアプリがあり、トップ画面のリンクで行き来できます。
そのほかに、記録を見る2つのページ（子ども向け kiroku.html、保護者向け oya.html）と、記録を保護者のスマホへ送るためのしくみ（dashboard/）があります。
GitHub Pages で公開している（https://ynghrksk-cmd.github.io/study-drill/）。

以下の前提・方針は、とくに書いていないかぎり **すべてのアプリに共通** です。理科・国語・社会・英語だけのきまりは、いちばん下のそれぞれの章に書いています。

## 前提（必ず守ること）

- **index.html の1ファイルで完結させる**
  - HTML・CSS・JavaScript はすべて index.html の中に書く。
  - 外部ライブラリ、CDN、ビルドツール、npm は使わない。ファイルも分割しない。
  - ネットにつながっていなくても、ファイルを開くだけで動くようにする。
  - **例外は「記録の送信」だけ**（下の章を見ること）。保護者が設定したときだけ、記録を家庭の Google Apps Script に送る。**設定していなければ、これまでどおり完全にオフラインで動く**。ドリルを解く動きが、送信の成否で止まることは絶対にない。
- **問題は自動生成する**
  - 問題をあらかじめ書いて並べておく方式にはしない。乱数で毎回ちがう問題を作る。
  - 1回の出題の中で、同じ式が重ならないようにする（`makeQuestions` の `seen`）。
- **答えは必ず整数にする**
  - 分数・小数・割り切れない答えが出ないようにする。
  - わり算や方程式は「答えを先に決めてから式を作る」ことで、整数になるのを保証する（例：`genEquation` は x を先に決める）。
  - 答えは「手書き（読みとり）」か「キー（0〜9・−・⌫）」で入力する。どちらも整数しか入力できない前提で UI を作っている。
- **1問ずつ答え合わせをして、まちがえたら とき方 を見せる**
  - とき方は中学1年生にもわかるように、1段階ずつ ていねいに書く（きまりの言葉 → 実際の式、の順）。
  - 問題を作るときに、同じ数からとき方も作る。とき方の式と答えが食いちがわないようにする。
- **計算スペースと答えの欄は、Apple Pencil（または指）で書ける**
  - 手書き数字の読みとりも外部ライブラリを使わず、index.html の中で行う（$P Point-Cloud Recognizer）。
  - 読みまちがいはキーで直せるようにする。1文字だけ直したときは、その字の形を localStorage に覚える（`math-drill-ink`）。
- **iPad の Safari で使う**
  - タッチ操作が基本。ボタンは指で押しやすい大きさにする。ホバーに頼る操作は作らない。
  - iPad の画面サイズ（縦向き・横向き）で見やすく表示されることを確認する。
  - 画面の拡大・縮小やスクロールの跳ね返りで、操作がじゃまされないようにする（viewport 設定、`overscroll-behavior`）。
  - Safari で使えない新しい機能は使わない。
  - パソコンのキーボードでも操作できる（数字・`-`・Backspace・Enter）が、これはおまけ。

## コードの構成

`<script>` の中は、次の順に並んでいる。

1. **問題を作る部分**：`rnd` / `rndNz` / `pick` などの補助関数、とき方の部品（`explainAdd` / `signNote` など）、単元ごとの `genXxx()`
   - `genXxx()` は `{ text, answer, prefix?, steps }` を返す。
   - `text` には `<small>` を入れてよい（「x ＝ 3 のとき」など）。
   - `answer` は整数。
   - `prefix` は答えの欄の前に出す文字（「x ＝」など）。
   - `steps` はとき方。`{ say: 説明（<b> で大事なきまりを強調）, math: 式（文字列か、行ごとの配列）, fig?: 図の HTML（数直線など） }` の配列。
   - 文章問題は `word: true` と、答えの単位 `suffix`（「℃」「個」など）をつける。`wordXxx()` で作り、各単元の `genXxx()` から `WORD_RATE`（4回に1回くらい）の割合で出す。
   - 文章問題のとき方は「文章を式にする → 計算する → 答え（単位つき）」の順。方程式の文章問題は「何を x とするか」から書く。
2. **`UNITS` 配列**：単元の一覧。単元を増やすときは、`steps` もつけた `genXxx()` を作って、ここに1行足す。
3. **手書き数字の読みとり**：`SHAPES`（数字の見本の形）、`cloud` / `classify`、`readInk`（左から1文字ずつに分けて読む）。
4. **手書きの線をかく**：`createInk()` が canvas を作る。線は点の配列で持ち、画面の大きさが変わったらかきなおす。ペンを使っている間は、指や手のひらのタッチを無視する。
5. **画面**：`state` オブジェクト1つで状態を持つ。`screenHome` / `screenQuiz` / `screenResult` が `#app` の `innerHTML` を書き換えて、画面を切り替える（切りかえる前に `resetScreen()`）。
   - iPad の縦向きは上から「問題 → 計算スペース → 答え」、横向きは「左：問題と計算スペース／右：答えと解説」。

## 書き方のルール

- 画面の文言は、中学1年生が読める、やさしい日本語にする。
- 数式の記号は全角でそろえる：`＋` `－` `−` `×` `÷` `＝`。負の数も、半角の `-` ではなく `−` で表示する。
- 色は CSS の `:root` にある変数を使う。色の意味は固定する：正解＝青（`--ok`）、まちがい＝オレンジ（`--ng`）、大事なきまり＝蛍光ペンの黄色（`--mark`）、次に押すボタン＝こい紺（`--primary`）。赤と緑の組み合わせは使わない（色覚のタイプによって見分けにくい）。

## 学習のための画面の方針（心理学・学習科学の知見にもとづく）

- 地は紙のような明るい色、文字はこい色にする（明るい地に暗い文字のほうが読みやすい）。飾りは少なくし、問題にいちばん目がいくようにする。
- まちがえたときは、責める言い方をしない。とき方は1行ずつ開き、「つぎはどうなる？」と考えてから次の行を見るようにする（自己説明）。
- 大事なきまりは、毎回同じ言い方にして `<b>`（蛍光ペン）で強調する。正負のたし算には数直線の図（`fig`）をつける。
- ほめるときは「頭がいい」ではなく、やり方やがんばりをほめる。
- 1問あたりの秒数や制限時間など、時間をせかす表示は出さない。
- 結果画面では、点数だけでなく「まちがえた問題をもう一度解く」「何日かあけて まぜこぜ で復習する」など、つぎにやることを伝える。

## 復習のしくみ（復習ボックス）＝ 5つのアプリで共通

とき方を読むだけでは定着しない。**自分の力で思い出すこと（検索練習）**と、**間をあけてくり返すこと（分散学習）**で記憶は強くなる。そのため、5つのアプリすべてに同じ「復習ボックス」を入れている。**どれかを直したら、ほかの4つにも同じ修正を入れる**。

- **項目（`item`）**：問題ごとではなく「きまり」ごとに記録する。すべての問題は `item`（キー）と `label`（一覧で見せる名前）を持つ。
  - 例：`term:光合成`／`kanji:把握`／`word:library`／`g:gVerbS`／`calc:calcDensity`／`addsub:word`
- **作りなおし（`regenFor(key)`）**：復習では**同じ問題ではなく、同じきまりから作った新しい問題**を出す。問題文ごと覚えると「解けた気」になるだけで、考え方が身につかないため。
  - データを持つ問題（用語・単語・漢字・文章）は、そのデータを引数にわたして作りなおす（`termChoice(t)` のように、引数がなければランダム）。
  - 数学の単元内の種類（`curKind`）と国語の文法のように、1つの関数が何種類も出し分けているものは、**ほしい種類が出るまで作りなおす**（作るのは軽い）。
  - `regenFor` は、**どの項目からも必ず問題を作れる**ようにする（作れない項目を増やさない）。
- **間かく（`BOX_STEPS = [1, 3, 7, 16, 35]`）**：まちがえたら「あした」、正解するたびに 3日後 → 1週間後 → 16日後 → 35日後 とのばし、5回できたら**卒業**（記録から消える）。
  - **同じ日に2回正解しても間かくは進めない**（間をあけることに意味があるため）。復習でまちがえたら、また最初（あした）にもどす。
- **その回の中でのもう一度（`queueRetry`）**：まちがえた問題は、すぐではなく **2〜3問あとに**、同じきまりの新しい問題として1回だけ出しなおす。出題画面には「もう一度」「復習」の目じるしを出す。
- **画面**：トップのいちばん上に「今日の復習 ○問」（`reviewHomeHTML`）、結果画面に復習ボックスのようす（`boxStateHTML`）を出す。復習の回では `state.mode === 'review'`。
- **記録の場所**：`localStorage` の `study-drill-review-<教科>`（`BOX_KEY`）。**この端末だけ**に残り、Safari のサイトデータを消すと記録も消える。読み書きは必ず try/catch で囲む（プライベートブラウズでも動くように）。

## 記録を見るページ（kiroku.html / oya.html）と、記録の送信

5つのドリルは、復習ボックスとは別に**学習の記録**を端末に残す。それを2つのページで見せる。

- **`kiroku.html`（子ども向け「がんばりの記録」）**：続けた日数・身についた数・今日の復習・4週間のカレンダー。**点数や正答率は前に出さない**（点を気にしすぎると、むずかしい問題をさけるようになるため）。5つのドリルのトップからリンクしている。いちばん下に保護者用の送信設定がある。
- **`oya.html`（保護者ページ）**：進み具合（教科ごとの正答率・項目の内わけ）、気をつけたい項目（まちがえた回数が多い順）、毎日の学習量。子どもの画面からはリンクしない（`kiroku.html` の設定の中からだけ開ける）。
- どちらも**1ファイルで完結**。CSS の変数と色の意味は5つのドリルと同じにそろえる。

### 記録の形（`study-drill-log-<教科>`）

- `ses`：1回ごと `[日, 分野, 復習か, 出題数, 正解数, 秒]`
- `item`：項目ごと `[出題数, 正解数, まちがい, 最後の日, 卒業した日, 分野]`
- `label`：項目の名前、`total`：その教科の全項目数（`ALL_ITEMS.length`）
- 日は「1970年から何日目か」（`today()`）。記録は**すべて残す**（消さない）。
- **項目を増やしたら `ALL_ITEMS` にも入れる**（進み具合の分母になる）。

### 送信（任意。設定したときだけ動く）

- 送信先の設定と、まだ送れていない記録は `study-drill-sync`（5教科で共通）に入れる。**URL と合言葉はリポジトリに置かない**（端末に入力する）。
- 1回終わるたびに `logSession` → `syncFlush`。失敗したらためておき、次にトップ画面を開いたときなどにまとめて送る。
- 送る内容は学習の記録だけ。氏名などの個人情報は送らない。
- 受け口は `dashboard/コード.gs`（Google Apps Script）。**直したら「デプロイ → デプロイを管理 → 新バージョン」まで必要**。手順は `dashboard/セットアップ.md`。
- `oya.html` の読みこみは、まず `fetch`、だめなら `<script>` で読む方法（JSONP）にする。**両方の道を残すこと**（Safari で CORS が通らない場合があるため）。読んだ記録は端末にも控えておき、オフラインでも最後の状態が見られるようにする。

### グラフの描き方

- 色の意味は固定：**身についた＝こい青（`--l5`）／できている＝うすい青（`--l3`）／復習中＝オレンジ（`--ng`）／まだ＝グレー（`--line`）**。学習量の濃淡は青1色のうすい→こい（`--l1`〜`--l5`）。
- 積み上げバーは**すべての区分に数字を添える**（色だけで意味を伝えない）。棒のあいだは 2px あける。
- 色覚のタイプによる見分けにくさは、思いこみで決めずに確かめること（青 `#2463A8` とオレンジ `#B8570E` は確認ずみ）。

## 理科ドリル（science.html）

- **science.html も1ファイルで完結させる**。index.html を読みこんだり、共通ファイルに分けたりしない。
  - 画面の CSS、手書き数字の読みとり（`SHAPES` / `readInk` など）、手書きの線（`createInk`）は、index.html と同じコードをコピーして使っている。**どちらかを直したら、もう一方にも同じ修正を入れる**。
  - 覚えた字の形（localStorage の `math-drill-ink`）は2つのアプリで共有される。
- 範囲は中1理科の4分野：植物のなかま（`plant`）／身のまわりの物質（`matter`）／光・音・力（`force`）／大地の変化（`earth`）、と まぜこぜ。
- 問題の形は3つ。1つの分野の中で、だいたい 計算 3割・えらぶ 4〜5割・思い出す 2〜3割（`TYPE_RATE`）。
  - `num`（計算）：数学と同じく、答えは整数で、手書きかキーで答える。答えを先に決めてから問題を作る（`CALC` の `calcXxx()`）。
  - `choice`（えらぶ）：選択肢をタップして「答え合わせ」。まちがいの選択肢は、同じ `group` の用語や、同じ分類のなかまの名前から選ぶ。
  - `recall`（思い出す）：頭の中で思い出してから「答えを見る」を押し、自分で ○×をつける（思い出す練習は記憶に残りやすい）。×のときは とき方 を見せる。
- **用語は乱数だけでは作れないので、例外として、きまりをデータで持つ**（`TERMS`：用語と説明、`SORTS`：分類）。
  - 1つのきまりから、出し方・選択肢の組み合わせ・順番を毎回かえて問題を作る。問題文そのものを並べたリストにはしない。
  - 用語や分類を足すときは、`group` ごとに4つ以上あると選択肢が4つそろう（3つ以下のときは選択肢も3つ以下になる）。
  - 説明（`desc` / `note` / `rule`）は教科書の内容に合わせ、まちがったことを覚えないように、あいまいなごろ合わせは書かない。

## 国語ドリル（kokugo.html）

- **kokugo.html も1ファイルで完結させる**。画面の CSS・手書き数字の読みとり・手書きの線（`createInk`）・画面のしくみは science.html と同じコードをコピーして使っている。**5つのうちどれかを直したら、ほかのファイルにも同じ修正を入れる**。
- 範囲は4つ：漢字（`kanji`）／語句：慣用句・ことわざ・四字熟語（`goku`）／文法：文節・主語・品詞・文節の関係（`bunpo`）／文章題：読解（`reading`）、と まぜこぜ。
- 問題の形は、理科と同じ `choice` / `recall` / `num` に、`write`（書く）を足した4つ。
  - `write`：漢字の書き取りと四字熟語の□。手書きの読みとりは数字しかできないので、**マスに書いてから「答えを見る」→ お手本（`model`）とくらべて自分で ○×** をつける。
  - `num`：文節の数を答える問題（手書き数字の読みとりを使う）。
- **国語のことばも乱数だけでは作れないので、理科と同じく、きまりをデータで持つ**：`KANJI`（ことば・読み・例文・意味）、`GOKU`（慣用句・ことわざ・四字熟語と意味）、`BUNSETSU` / `HINSHI` / `KANKEI`（文法）。
  - 意味が同じ語句（「猿も木から落ちる」と「弘法にも筆の誤り」など）は、まちがいの選択肢に入れない（`gokuQuestion` の `same`）。
  - 漢字は中学で習う常用漢字をふくむことばから選ぶ。教科書によって出てくる学年がちがうので、学校の教科書とちがうものがあれば入れかえる。
- **文章題（`PASSAGES`）は、このアプリのために書いたオリジナルの短い文章だけを使う**。教科書や本の文章を写さない（著作権のため）。
  - 1つの文章に問いを2〜3つつけ、とき方（`why`）には「どこに注目すれば答えがわかるか」（接続語、はじめと終わりの気持ち、「このように」のあとの筆者の考え など）を書く。
  - 文章題のときは、左がわに本文（`passage`）を表示し、メモスペースは小さくする。
- コメントは日本語で書く。

## 社会ドリル（shakai.html）

- **shakai.html も1ファイルで完結させる**。画面の CSS・手書き数字の読みとり・手書きの線（`createInk`）・画面のしくみは science.html / kokugo.html と同じコードをコピーして使っている。**5つのうちどれかを直したら、ほかのファイルにも同じ修正を入れる**。
- 範囲は4つ：世界と日本のすがた（`sekai`）／世界の人々のくらし（`kurashi`）／古代までの日本（`kodai`）／武士の世の中（`chusei`：鎌倉・室町）、と まぜこぜ。地理2つ・歴史2つ。
- 問題の形は、国語ドリルと同じ4つ（`num` / `choice` / `recall` / `write`）。1つの分野の中で、だいたい 計算 2〜3割・えらぶ 4〜5割・漢字で書く 1割・思い出す 2割（`TYPE_RATE`）。
  - `num`（計算）：数学・理科と同じく、答えは必ず整数。答えを先に決めてから数を作る（`CALC` の `calcXxx()`）。時差・標準時子午線・縮尺・人口密度・割合・世紀・年数。
  - `choice`（えらぶ）：用語（`termChoice`）、分類（`sortChoice`）に、分野ごとの問題（県庁所在地・雨温図・できごとの前後）をまぜて出す。
  - `recall`（思い出す）／`write`（漢字で書く）：`write` は、読み（`y`）をつけた用語だけから出す。マスに書いて、お手本とくらべて自分で ○×。**お手本がはみ出さないよう、`y` をつけるのは5字までの用語にする**。
- **社会の用語は乱数だけでは作れないので、理科・国語と同じく、きまりをデータで持つ**：`TERMS`（用語・説明・補足・読み）、`SORTS`（分類）、`EVENTS`（年表）、`KENCHO`（県庁所在地）、`CLIMATE`（雨温図のもとの数値）。
  - 1つのきまりから、出し方・選択肢の組み合わせ・順番を毎回かえて問題を作る。問題文そのものを並べたリストにはしない。
  - `TERMS` の `desc` は「〜を何といいますか。」の「〜」に入るので、**かならず名詞で終える**。
  - 選択肢のまちがいは同じ `group` の用語から選ぶので、`group` ごとに4つ以上あるとよい。
  - 年号・用語は教科書の内容に合わせる。領土の記述など、まちがったことを覚えないように、あいまいな書き方はしない。
- **雨温図（`CLIMATE` → `climateChart`）は SVG をその場で組み立てる**。画像ファイルや外部ライブラリは使わない。
  - 気温は折れ線（−30〜30℃の目もり）、降水量は棒（0〜最大値の目もり）。出すたびに数を少しだけゆらすので、同じ都市でも同じグラフにはならない。**ゆらしても気候帯の見分け方が変わらない範囲にする**。
  - 資料のある問題は `shiryo` を持たせると、左がわに資料、右がわに答えが出る（`has-shiryo`）。
- コメントは日本語で書く。

## 英語ドリル（eigo.html）

- **eigo.html も1ファイルで完結させる**。画面の CSS・手書き数字の読みとり・手書きの線（`createInk`）・画面のしくみは science.html / kokugo.html / shakai.html と同じコードをコピーして使っている。**5つのうちどれかを直したら、ほかのファイルにも同じ修正を入れる**。
- 範囲は4つ：単語（`word`）／文法（`bunpo`）／会話表現と数（`hyogen`）／英文を読む（`reading`）、と まぜこぜ。
- 問題の形は4つ（`choice` / `recall` / `write` / `num`）。分野ごとに出し方がちがう。
  - `write`（英語で書く）：**4本線のマス**に手書きして、お手本とくらべて自分で ○×。手書きの読みとりは数字しかできないので、英語は自己採点にする。お手本がはみ出さないよう、**書かせる語は11字くらいまで**にする。
  - `num`（数字で答える）：英語の数・曜日や月の順番・日づけ・時こくを数字で答える（手書き数字の読みとりを使う）。答えは必ず整数。
  - `choice` / `recall`：意味・つづり・文法のきまり・会話の返事など。
- **文法の問題は、データから文を組み立てて作る**（問題文を並べたリストにしない）。
  - `SUBJ`（主語＋be動詞＋3人称単数かどうか）、`VERB`（動詞の変化形と、その作り方 `r3` / `ri` / `rp`、あとに続く語句、日本語の言い方）を毎回えらんで文を作り、とき方も同じデータから作る。
  - 動詞の日本語は `jp`（〜ます）を持ち、否定は `jNeg`、過去は `jPast` で作る。**「〜ています」だけは五段動詞で形が変わるので `ji` に直接書く**（作ります → 作っています）。can の文は辞書形 `jd` を使う。
  - まちがいの選択肢も、同じ動詞・同じ代名詞のちがう形から作る（当てずっぽうで当たらないようにする）。
  - 語順の問題（`gOrder`）では、**「毎日」などの語句が文のはじめに来る並べかえを、まちがいの選択肢にしない**（英語では正しい文になってしまうため）。
- **単語（`WORDS`）・会話表現（`TALK` / `PHRASE`）・読解の英文（`PASSAGES`）はデータで持つ**。
  - `WORDS` は `c`（なかま）ごとにまとめ、まちがいの選択肢は同じなかまから選ぶ。例文 `s` と、その意味 `sj` もつける。
  - **英文（`PASSAGES`）は、このアプリのために書いたオリジナルの短い英文だけを使う**。教科書や本の英文を写さない（著作権のため）。問いは日本語で出し、とき方（`why`）には「英文のどこを見れば答えがわかるか」を書く。
  - 英文は `.en` / `.sent` の書体（英語が読みやすい書体）で表示する。日本語の意味は `.mean` で小さく そえる。
- コメントは日本語で書く。
